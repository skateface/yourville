---
layout: base.njk
title: Guides
description: Every Yourville guide — small towns sorted by the kind of life you're actually looking for.
permalink: /guides/
---
<div class="article-header">
  <div class="container">
    <h1>Guides</h1>
    <p class="article-meta">Eight town archetypes, each with real towns worth a look.</p>
  </div>
</div>
<section>
  <div class="container">
    <div class="card-grid">
      {% for a in archetypes %}
      <a class="card" href="{{ a.guide }}">
        <span class="emoji">{{ a.emoji }}</span>
        <h3>{{ a.name }}</h3>
        <p>{{ a.description }}</p>
      </a>
      {% endfor %}
    </div>
  </div>
</section>
